"""Private production deployment controller.

This package owns deployment state, release admission, transaction classification,
and durable terminal contracts. Product source/build/release logic remains in
`iamaman11/mobile-proxy`.
"""
